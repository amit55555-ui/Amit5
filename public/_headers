# Never cache HTML/documents so a new deploy is always picked up immediately
# (prevents the "blank page after update" caused by stale HTML referencing old chunks).
/*
  Cache-Control: no-cache, must-revalidate

# Hashed static assets can be cached forever (their filename changes on every build).
/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
