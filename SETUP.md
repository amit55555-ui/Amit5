# מפת נזילות מים — תל אביב

אתר פשוט עם מפה של תל אביב-יפו שבו כל אחד יכול לסמן, **בעילום שם**, מיקום
של נזילה או טפטוף מים, לצרף תמונה ולפרסם אותה כדי שכולם יראו אותה על המפה.

## הפעלה מקומית

```bash
npm install
npm run dev
```

פתחו <http://localhost:3000>. האפליקציה עובדת **מיד** ללא הגדרות:
- כל מבקר רואה על המפה את כל הדיווחים הקיימים.
- לחיצה על "דיווח על נזילה" ואז לחיצה במקום המתאים על המפה פותחת טופס
  לצירוף תמונה (חובה) ותיאור קצר (רשות), ומפרסמת את הדיווח מיד לכולם.
- אין הרשמה, התחברות או פרטים אישיים — הדיווח אנונימי לחלוטין.

> במצב הבסיסי (בלי הגדרת מסד נתונים) הדיווחים נשמרים בזיכרון השרת בלבד
> ונמחקים בכל הפעלה מחדש. לשימוש אמיתי חברו מסד נתונים (ראו למטה).

## פריסה (Vercel) — אחסון אמיתי ומשותף

1. דחפו ל-GitHub וייבאו את הפרויקט ב-Vercel.
2. הוסיפו אינטגרציית **Neon** או **Vercel Postgres** לפרויקט — זה יגדיר
   אוטומטית את `DATABASE_URL` וכל הדיווחים יישמרו לצמיתות ויהיו גלויים
   לכל המבקרים.
3. הוסיפו אינטגרציית **Vercel Blob** — התמונות יועלו אליה במקום להישמר
   כ-data URL ישירות במסד הנתונים (מהיר וזול יותר).

בלי שתי האינטגרציות האפליקציה עדיין עובדת (עם נפילה חזרה לזיכרון /
לשמירת התמונה כ-data URL), אבל לשימוש אמיתי מומלץ לחבר אותן.

## מבנה הפרויקט

| נתיב | תיאור |
|------|-------|
| `app/page.tsx` | המסך הראשי — המפה, כפתור הדיווח וזרימת הדיווח |
| `components/LeakMap.tsx` | רכיב המפה (Leaflet) והצגת הסימונים |
| `components/ReportLeakForm.tsx` | טופס דיווח נזילה (תמונה + תיאור) |
| `lib/leaks-store.ts` | מחסן הדיווחים בצד השרת (Postgres / זיכרון) |
| `lib/photos.ts` | העלאת תמונות ל-Vercel Blob |
| `lib/image.ts` | דחיסת התמונה בצד הלקוח לפני שליחה |
| `app/api/leaks/route.ts` | API לרשימת דיווחים וליצירת דיווח חדש |
